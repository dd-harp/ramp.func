# Make a Zero Truncated Negative Binomial

Return a zero truncated negative binomial distribution with a given mean
and size

## Usage

``` r
make_NBtrunc(mu, size)
```

## Arguments

- mu:

  the mean value

- size:

  the size parameter

## Value

a probability
