% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_ts_function.R
\name{make_ts_function}
\alias{make_ts_function}
\title{Make a Composed Time Series Function}
\usage{
make_ts_function(
  avg = 1,
  season_par = list(),
  trend_par = list(),
  shock_par = list(),
  times = c(0, 365),
  norm_with_shocks = FALSE,
  N = 1,
  form = "tV",
  options = list()
)
}
\arguments{
\item{avg}{the average}

\item{season_par}{seasonality function for}

\item{trend_par}{trend function parameters}

\item{shock_par}{trend function parameters}

\item{times}{normalize \eqn{t_0 = \mbox{min(times)}} to \eqn{t_1 = \mbox{max(times)}}}

\item{norm_with_shocks}{if FALSE, set {\eqn{F_K(t)=1}} for normalization}

\item{N}{the length of the return value}

\item{form}{functional form: "t" returns \eqn{F(t)}; "tV" returns \eqn{F(t,V)}}

\item{options}{configurable options}
}
\value{
a function
}
\description{
Build a function that returns
a product of four configurable elements:
\itemize{
\item \eqn{\bar x}: a mean value
\item \eqn{F_S(t)}: a seasonal pattern
\item \eqn{F_T(t)}: a trend
\item \eqn{F_K(t)}: a shock
}

The return value is a function of the form:
\itemize{
\item \eqn{F_t(t)} if \code{form == "t"}
\item \eqn{F_t(t, V(t))} if \code{form == "tV"}
}

The user can pass any vector \code{times} and the interval is
set to
\itemize{
\item \eqn{t_0 = \mbox{min(times)}}, and
\item \eqn{t_1 = \mbox{max(times)}}.
}

A normalizing constant is set such that the daily average over
the interval \eqn{(t_0, t_1)} is \eqn{\bar x}:
\deqn{\int_{t_0}^{t_1} F_t(t) dt = \bar x \left(t_1-t_0\right).}

The component functions are specified by
passing \code{F_obj} objects (see \link{F_obj}):
\itemize{
\item \code{season_par} creates \eqn{F_S(t)} or \code{F_season} (\emph{eg,} using \link{makepar_F_sin})
\item \code{trend_par} creates \eqn{F_T(t)} or \code{F_trend} (\emph{eg,} using \link{makepar_F_spline})
\item \code{shock_par} creates \eqn{F_K(t)} or \code{F_shock} (\emph{eg,} using \link{makepar_F_sharkbite})
}
}
