% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_F_age.R
\name{make_F_a}
\alias{make_F_a}
\title{Make a Function,}
\usage{
make_F_a(
  A = 5 * 365,
  bday = 0,
  avg = 1,
  age_par = makepar_F_type2(),
  season_par = makepar_F_c(1),
  trend_par = makepar_F_c(1),
  shock_par = makepar_F_c(1),
  norm_with_shocks = TRUE,
  times = NULL,
  form = "t",
  options = list()
)
}
\arguments{
\item{A}{the maximum age (in days)}

\item{bday}{the cohort birthday \eqn{(d)}}

\item{avg}{the average exposure, \eqn{\bar E}}

\item{age_par}{an \link{F_obj} for relative biting rate by age to construct \eqn{F_\omega} (see \link{age})}

\item{season_par}{an \link{F_obj} for the seasonal pattern to construct \eqn{F_S} (see \link{seasonality})}

\item{trend_par}{an \link{F_obj} for the trend to construct \eqn{F_T} (see \link{trends})}

\item{shock_par}{an \link{F_obj} for to construct a perturbation function \eqn{F_K} (see \link{shocks})}

\item{norm_with_shocks}{if FALSE, set {\eqn{F_K(t)=1}} for normalization}

\item{times}{if not NULL, normalize \eqn{F_t} from \eqn{t_0 = \mbox{min(times)}} to \eqn{t_1 = \mbox{max(times)}}}

\item{form}{functional form: "t" returns \eqn{F(t)}; "tV" returns \eqn{F(t,V)}}

\item{options}{a list of setup options}
}
\value{
a function
}
\description{
Construct a trace function to simulate
exposure by age, \eqn{a}, in a cohort born on day \eqn{d},
(and since age is related to time, \eqn{t}, by the formula \eqn{t = a+d}), then we compute

\deqn{F(a, d) =
\begin{cases}
0 & \mbox{if } a <0 \\
\bar E \times F_\omega(a) \times F_t(a+d) & \mbox{if } a >=0
\end{cases}.
}

and
\itemize{
\item \eqn{\bar E} --- or \code{avg} the approximate mean value
\item \eqn{F_\omega(a)} --- a function describing the relative biting rate by age
\item \eqn{F_t(t)} --- the temporal pattern function.
}

The temporal pattern function \eqn{F_t} is a composed time series function:
\deqn{F_t(t) = x \times F_S(t) \times F_T(t) \times F_K(t)}

where
\itemize{
\item x --- a normalizing constant
\item \eqn{F_S(t)} --- a seasonal pattern function
\item \eqn{F_T(t)} --- a trend pattern function
\item \eqn{F_K(t)} --- a shock function
}

The normalizing constant is set such that the daily average over the interval is 1:
\deqn{\int_{t_0}^{t_1} F_t(t) dt = t_1-t_0.}

By default \eqn{t_0=d} and \eqn{t_1=d+A}, but if the user
passes a non-NULL value for \code{times} then the boundaries for
the normalizing interval are
set to
\itemize{
\item \eqn{t_0 = \mbox{min(times)}}, and
\item \eqn{t_1 = \mbox{max(times)}}.
}

For convenience in constructing counterfactuals, normalization can be done
with or without the shock function.

By default, \eqn{d=0}
}
\note{
A named list of options is an alternative way of passing arguments.
}
